/**
 * Shared utilities for component tests.
 *
 * @example
 * import {
 *   mountPage,
 *   cleanupAfterTest,
 *   wrapper,
 *   waitFor,
 *   waitForElement,
 *   waitForText,
 *   mockUser,
 * } from '~~/tests/component';
 */

export * from './mocks';
export * from './mount';
export * from './wait';
